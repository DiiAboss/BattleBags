/// @description
/// @description
if (keyboard_check(vk_down))
{
	sprite = down_sprite;
}

if (keyboard_check(vk_left))
{
	sprite = left_sprite;
}

if (keyboard_check(vk_right))
{
	sprite =right_sprite;
}